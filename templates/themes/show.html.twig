{% extends 'base.html.twig' %}

{% block title %}Themes{% endblock %}

{% block body %}
    <h1>Themes</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ theme.id }}</td>
            </tr>
            <tr>
                <th>TitleTheme</th>
                <td>{{ theme.titleTheme }}</td>
            </tr>
            <tr>
                <th>PictureTheme</th>
                <td>{{ theme.pictureTheme }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('themes_index') }}">back to list</a>

    <a href="{{ path('themes_edit', {'slug': theme.id}) }}">edit</a>

    {{ include('themes/_delete_form.html.twig') }}
{% endblock %}
